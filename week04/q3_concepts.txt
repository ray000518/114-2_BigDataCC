姓名：黃亭睿
學號：C112181129

A1：管線符號的作用是將前一個指令的 「輸出結果」（stdout），直接傳送給下一個指令當作 「輸入來源」（stdin）。這讓你可以像組裝樂高一樣，把多個簡單的指令串接起來處理複雜的任務。
ls -l | less

A2:>	覆蓋 (Overwrite)用來建立一個全新的設定檔，或是重置日誌檔。,>>	累加 (Append)用來記錄 Log（日誌），保留舊訊息並增加新內容。

A3:grep "/bin/bash" /etc/passwd | wc -l